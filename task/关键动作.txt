读取操作流、成员清单与因果策略，确认操作主键、修订、成员序号、依赖向量和权限规则。
按op_id比较revision与ingest_seq，选定规范修订并为旧修订记录替代关系。
为每名成员维护时钟，只将本人下一序号且全部依赖满足的规范操作加入就绪集合。
按照策略顺序选择下一条就绪操作，检查账户状态、角色权限和目标块状态。
对accepted的insert、edit、delete更新块集合，对accepted的comment写入独立评论集合。
让accepted与rejected推进成员时钟，同时阻止rejected改变块和评论。
按ingest_seq生成裁决台账，按首次插入次序生成活跃块快照，并汇总成员时钟。
生成对账摘要，核对原始行、规范操作、终态、快照和成员时钟的数量关系。
