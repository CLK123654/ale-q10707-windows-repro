操作系统： Windows11、macOS或主流64位Linux。

主软件为Node.js20或更高兼容版本，需支持NDJSON解析、Map与Set、稳定排序、文件系统和子进程标准模块。程序只读取随包提供的操作流、成员清单和因果策略，处理期间不联网。

执行账号需要读取input_data中的data、rules、starter和入口，并能在同级output目录写入源码与报告。当前输入规模适合普通64位CPU与常规办公内存，不使用GPU；磁盘需要容纳输入包、状态快照、裁决台账和五件交付。需设置有限超时，覆盖本次因果回放并避免异常无限等待。
